{
  "name": "ai-agency-platform",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "cross-env NODE_OPTIONS=--max-old-space-size=4096 concurrently \"npm --workspace=apps/api run dev\" \"npm --workspace=apps/portal run dev\"",
    "build": "npm run build --workspace=packages/shared && npm run build --workspace=apps/api && npm run build --workspace=apps/portal",
    "db:generate": "prisma generate",
    "db:migrate": "prisma migrate dev",
    "db:push": "prisma db push"
  },
  "devDependencies": {
    "@prisma/client": "^6.6.0",
    "concurrently": "^8.2.2",
    "prisma": "^6.6.0",
    "typescript": "^5.3.3"
  },
  "overrides": {
    "@types/googleapis": "npm:@types/node@^20"
  },
  "dependencies": {
    "next": "14.2.35",
    "nodemailer": "8.0.2"
  }
}
